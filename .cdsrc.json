{
  "build": {
    "target": "."
  },
  "requires": {
    "db": {
      "kind": "hana",
      "[mock]": {
        "kind": "sqlite",
        "impl": "@cap-js/sqlite",
        "model": ["db_mock"],
        "credentials": {
          "url": "db.sqlite"
        }
      }
    }
  }
}
